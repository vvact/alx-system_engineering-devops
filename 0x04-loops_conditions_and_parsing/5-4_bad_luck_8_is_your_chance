#!/usr/bin/env bash
# This script display different messages based on the loop iteration

r=1
while [ $r -le 10 ];
do
	if [ $r -eq 4 ];
	then
		echo "bad luck"
	elif [ $r -eq 8 ];
	then
		echo "good luck"
	else
		echo "Best School"
	fi
	((r++))
done
